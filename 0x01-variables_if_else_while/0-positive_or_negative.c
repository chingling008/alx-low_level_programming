#include <stdio.h>

int main(void) {
 int n  = rand()

    if(n > 0) {
         printf("%d is postive\n", n);
    } 
    else if (n = 0) {
       printf("%d is zero\n", n);
    }
   else if (n < 0) {
       printf("%d is negative\n", n);
    }
    return 0;
}
